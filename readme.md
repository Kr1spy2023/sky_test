# 🚀 Sky Test

<div align="center">

**Современная веб-платформа для создания и прохождения онлайн-тестов**

[![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)](https://www.python.org/)
[![Flask](https://img.shields.io/badge/Flask-3.0.0-green.svg)](https://flask.palletsprojects.com/)
[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

</div>

---

## 📋 Описание

**Sky Test** — это полнофункциональное веб-приложение для создания тестов, проверки знаний и отслеживания результатов. Платформа позволяет преподавателям легко создавать тесты с различными типами вопросов, а студентам — проходить их с любого устройства.

### ✨ Основные возможности

- 🎯 **Создание тестов** — интуитивный конструктор для создания тестов с вопросами разных типов
- 📝 **Типы вопросов** — поддержка вопросов с одним вариантом ответа, множественным выбором и текстовых ответов
- 📊 **Детальная статистика** — подробные отчеты по результатам прохождения тестов
- 🔗 **Публичные ссылки** — делитесь тестами по уникальной ссылке, доступной без регистрации
- 👤 **Управление профилем** — персональный кабинет с настройками и историей тестов
- 🔐 **Безопасность** — JWT аутентификация и хеширование паролей
- 📱 **Адаптивный дизайн** — удобная работа с любого устройства

---

## 🛠 Технологии

### Backend
- **Flask 3.0.0** — веб-фреймворк
- **SQLAlchemy 3.1.1** — ORM для работы с базой данных
- **SQLite** — база данных (легко переключается на PostgreSQL/MySQL)
- **PyJWT 2.8.0** — JWT токены для аутентификации
- **Werkzeug 3.0.1** — безопасное хеширование паролей
- **Flasgger 0.9.7.1** — автодокументация API (Swagger)
- **Flask-CORS 4.0.0** — поддержка CORS для API

### Frontend
- **Jinja2** — шаблонизатор
- **HTML5/CSS3** — современная верстка
- **JavaScript** — интерактивность интерфейса

---

## 📁 Структура проекта

```
sky_test/
├── app.py                      # Точка входа приложения
├── config.py                   # Конфигурация приложения
├── requirements.txt            # Зависимости Python
│
├── backend/
│   ├── models/                 # Модели базы данных
│   │   ├── user.py            # Модель пользователя
│   │   ├── test.py            # Модель теста
│   │   ├── question.py        # Модель вопроса
│   │   ├── attempt.py         # Модель попытки прохождения
│   │   └── answer.py          # Модель ответа
│   │
│   ├── routes/                 # Маршруты (blueprints)
│   │   ├── auth.py            # API аутентификации
│   │   ├── tests.py           # API управления тестами
│   │   ├── questions.py       # API управления вопросами
│   │   ├── attempts.py        # API прохождения тестов
│   │   ├── statistics.py      # API статистики
│   │   └── views.py           # HTML страницы
│   │
│   ├── services/               # Бизнес-логика
│   │   ├── auth_service.py
│   │   ├── test_service.py
│   │   ├── attempt_service.py
│   │   └── stats_service.py
│   │
│   └── utils/                  # Утилиты
│       ├── jwt_utils.py        # Работа с JWT токенами
│       ├── password.py         # Хеширование паролей
│       ├── validation.py       # Валидация данных
│       └── responses.py        # Стандартизированные ответы API
│
├── database/
│   ├── init_db.py             # Инициализация БД
│   └── tests.db               # База данных SQLite
│
├── templates/                  # HTML шаблоны
│   ├── base.html
│   ├── index.html
│   ├── login.html
│   ├── register.html
│   ├── dashboard.html
│   ├── create_test.html
│   ├── edit_test.html
│   ├── take_test.html
│   ├── test_result.html
│   ├── statistics.html
│   └── settings.html
│
└── static/                     # Статические файлы
    ├── css/                   # Стили
    └── js/                    # JavaScript
```

---

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)

### Установка

1. **Клонируйте репозиторий** (или скачайте архив):
   ```bash
   git clone <repository-url>
   cd sky_test
   ```

2. **Создайте виртуальное окружение** (рекомендуется):
   ```bash
   python -m venv venv
   
   # Windows
   venv\Scripts\activate
   
   # Linux/Mac
   source venv/bin/activate
   ```

3. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройте переменные окружения** (опционально):
   
   Создайте файл `.env` в корне проекта:
   ```env
   SECRET_KEY=your-secret-key-here
   DATABASE_URL=sqlite:///database/tests.db
   JWT_EXPIRATION_HOURS=24
   CORS_ORIGINS=http://localhost:8080
   FLASK_DEBUG=False
   FLASK_HOST=127.0.0.1
   FLASK_PORT=8000
   ```
   
   > 💡 Если файл `.env` не создан, будут использованы значения по умолчанию

5. **Инициализируйте базу данных**:
   
   База данных создастся автоматически при первом запуске, но можно инициализировать вручную:
   ```bash
   python -c "from database.init_db import init_database; from app import app; init_database(app)"
   ```

### Запуск приложения

```bash
python app.py
```

Приложение будет доступно по адресу: **http://127.0.0.1:8000**

---

## 📖 Использование

### Веб-интерфейс

1. **Регистрация/Вход**:
   - Перейдите на главную страницу
   - Зарегистрируйтесь или войдите в систему
   - После входа вы попадете в личный кабинет

2. **Создание теста**:
   - В личном кабинете нажмите "Создать тест"
   - Заполните название и описание теста
   - Добавьте вопросы различных типов:
     - **Одиночный выбор** — один правильный ответ
     - **Множественный выбор** — несколько правильных ответов
   - Сохраните как черновик или опубликуйте сразу

3. **Прохождение теста**:
   - Скопируйте ссылку на опубликованный тест
   - Поделитесь ссылкой со студентами
   - Студенты могут пройти тест без регистрации (результаты сохраняются только для зарегистрированных пользователей)

4. **Статистика**:
   - Просматривайте статистику по каждому тесту
   - Анализируйте результаты прохождения
   - Отслеживайте средний балл и количество попыток

### API

Приложение предоставляет REST API для интеграции с другими системами.

#### Документация API

После запуска приложения документация Swagger доступна по адресу:
**http://127.0.0.1:8000/swagger**

#### Основные endpoints:

- `POST /api/auth/register` — регистрация пользователя
- `POST /api/auth/login` — вход в систему
- `GET /api/auth/profile` — получение профиля (требует аутентификации)
- `PUT /api/auth/profile` — обновление профиля (требует аутентификации)
- `GET /api/tests` — список тестов пользователя
- `POST /api/tests` — создание теста
- `GET /api/tests/{id}` — получение теста
- `PUT /api/tests/{id}` — обновление теста
- `DELETE /api/tests/{id}` — удаление теста
- `POST /api/tests/{id}/publish` — публикация теста
- `GET /api/tests/link/{token}` — получение теста по публичной ссылке
- `POST /api/tests/{id}/questions` — создание вопроса
- `POST /api/tests/{test_id}/attempts` — начало попытки прохождения
- `POST /api/attempts/{id}/finish` — завершение попытки
- `GET /api/attempts/{id}/results` — получение результатов
- `GET /api/tests/{id}/statistics` — статистика по тесту

> 🔐 Все API endpoints (кроме регистрации, входа и получения теста по ссылке) требуют JWT токен в заголовке `Authorization: Bearer <token>`

---

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `SECRET_KEY` | Секретный ключ для подписи сессий и токенов | `dev-secret-key-change-in-production` |
| `DATABASE_URL` | URL базы данных | `sqlite:///database/tests.db` |
| `JWT_EXPIRATION_HOURS` | Срок действия JWT токена (часы) | `24` |
| `CORS_ORIGINS` | Разрешенные домены для CORS (через запятую) | `http://localhost:8080` |
| `FLASK_DEBUG` | Режим отладки | `False` |
| `FLASK_HOST` | Хост для запуска сервера | `127.0.0.1` |
| `FLASK_PORT` | Порт для запуска сервера | `8000` |

---

## 🔒 Безопасность

- ✅ Пароли хешируются с использованием bcrypt (через Werkzeug)
- ✅ JWT токены для безопасной аутентификации
- ✅ Валидация всех входных данных
- ✅ Защита от CSRF (через сессии Flask)
- ✅ Ограничение размера запросов (16MB)
- ✅ CORS настройки для API

> ⚠️ **Важно**: В продакшене обязательно измените `SECRET_KEY` на случайную строку!

---

## 🧪 Разработка

### Режим отладки

Для включения режима отладки установите в `.env`:
```env
FLASK_DEBUG=True
```

### Структура кода

- **Models** (`backend/models/`) — модели базы данных SQLAlchemy
- **Routes** (`backend/routes/`) — обработчики HTTP запросов (Flask Blueprints)
- **Services** (`backend/services/`) — бизнес-логика приложения
- **Utils** (`backend/utils/`) — вспомогательные утилиты

---

## 📝 Лицензия

Этот проект распространяется под лицензией MIT.

---

## 👤 Автор

Разработано с ❤️ для образовательных целей

---

<div align="center">

**Создавайте тесты легко, проходите их эффективно! 🎓**

</div>

